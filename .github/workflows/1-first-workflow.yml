# name
name: First worflow

# event
on:
  push:
    branches:
      - main

jobs:
  # job
  first-workflow:
    # runner - OS
    runs-on: ubuntu-latest

    # steps of actions
    steps:
      - name: First step
        run: |
          echo "hello, world!"
